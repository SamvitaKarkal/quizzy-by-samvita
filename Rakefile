# Add your own quizzes in files placed in lib/quizzes ending in .rake,
# for example lib/quizzes/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_quizzes
